/* parser.js — lógica do parser de texto colado */
/* Implementação completa virá na próxima etapa */

const Parser = (() => {
  function parse(raw) { return {}; }
  function parseViews(str) { return null; }
  function formatViews(n) { return ''; }
  return { parse, parseViews, formatViews };
})();
